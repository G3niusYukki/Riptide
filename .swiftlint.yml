# SwiftLint configuration for Riptide

disabled_rules:
  - trailing_comma
  - opening_brace

opt_in_rules:
  - closure_spacing
  - empty_count
  - explicit_init
  - fatal_error_message
  - first_where
  - modifier_order
  - toggle_bool

line_length:
  warning: 150
  error: 200
  ignores_comments: true

file_length:
  warning: 500
  error: 1000

type_body_length:
  warning: 400
  error: 600

function_body_length:
  warning: 80
  error: 120

excluded:
  - vendor/
  - .build/
  - DerivedData/
  - Tests/
